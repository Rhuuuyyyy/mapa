# ============================================================================
# MAPA SaaS v2.0.0 - Environment Variables Template
# Copie para .env e preencha os valores
#
# ⚠️  SEGURANÇA: Nunca commite o arquivo .env no repositório!
# ============================================================================

# ============================================================================
# DATABASE
# ============================================================================
# Use credenciais fortes e diferentes para cada ambiente
DATABASE_URL=postgresql://user:SENHA_FORTE_AQUI@localhost:5432/mapa_db

# ============================================================================
# SEGURANÇA - CONFIGURAÇÕES CRÍTICAS
# ============================================================================

# SECRET_KEY: OBRIGATÓRIO - Gere uma chave única e segura!
# Execute: python -c "import secrets; print(secrets.token_urlsafe(64))"
# NUNCA use a chave de exemplo em produção!
SECRET_KEY=GERE_UMA_CHAVE_SEGURA_COM_O_COMANDO_ACIMA

# Algoritmo JWT (não alterar a menos que necessário)
ALGORITHM=HS256

# Tempo de expiração do token em minutos (padrão: 30)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================================================
# APLICAÇÃO
# ============================================================================

# DEBUG: DEVE ser False em produção!
# True expõe informações sensíveis e permite CORS wildcard
DEBUG=False

# CORS - Origens permitidas
# ⚠️  NUNCA use "*" em produção! Isso permite qualquer site fazer requisições.
# Liste explicitamente os domínios permitidos, separados por vírgula:
ALLOWED_ORIGINS=https://seu-dominio.com,https://www.seu-dominio.com
# Para desenvolvimento local, adicione também:
# ALLOWED_ORIGINS=http://localhost:3000,https://seu-dominio.com

# ============================================================================
# UPLOAD DE ARQUIVOS
# ============================================================================

# Tamanho máximo de upload em bytes (padrão: 10MB)
MAX_UPLOAD_SIZE=10485760

# Diretório de uploads (relativo à raiz do projeto)
UPLOAD_DIR=uploads

# ============================================================================
# AZURE (se aplicável)
# ============================================================================

# Porta do servidor (Azure define automaticamente via WEBSITES_PORT)
WEBSITES_PORT=8000

# ============================================================================
# SETUP INICIAL (usar apenas na primeira configuração!)
# ============================================================================

# Habilita endpoint de criação do primeiro admin
# ⚠️  REMOVA ou defina como False após criar o primeiro admin!
ALLOW_ADMIN_SETUP=false

# ============================================================================
# FRONTEND (variáveis VITE_* são expostas ao frontend)
# ============================================================================

# Email de suporte exibido na página de login
VITE_SUPPORT_EMAIL=suporte@sua-empresa.com.br

# URL da API (opcional - padrão usa /api em produção)
# VITE_API_URL=https://api.seu-dominio.com
